// Code generated by protoc-gen-kratos-foundation-errors-v2. DO NOT EDIT.

package kratos_foundation_pb

import (
	fmt "fmt"
	errors "github.com/jaggerzhuang1994/kratos-foundation/v2/pkg/errors"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the kratos package it is being compiled against.
const _ = errors.SupportPackageIsVersion1

// IsUndefined 判断 err 是否为 UNDEFINED 错误。
// 业务说明：成功
func IsUndefined(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "UNDEFINED" && e.Metadata != nil && e.Metadata["reason_code"] == "0"
}

// ErrorUndefined 创建 UNDEFINED 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：成功
func ErrorUndefined(messageParts ...any) *errors.Error {
	message := "成功"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(200, "UNDEFINED", message).WithReasonCode(0).WithErrStack(4)
}

// IsBadRequest 判断 err 是否为 BAD_REQUEST 错误。
// 业务说明：异常请求
func IsBadRequest(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "BAD_REQUEST" && e.Metadata != nil && e.Metadata["reason_code"] == "400"
}

// ErrorBadRequest 创建 BAD_REQUEST 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：异常请求
func ErrorBadRequest(messageParts ...any) *errors.Error {
	message := "异常请求"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(400, "BAD_REQUEST", message).WithReasonCode(400).WithErrStack(4)
}

// IsUnauthorized 判断 err 是否为 UNAUTHORIZED 错误。
// 业务说明：无效身份
func IsUnauthorized(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "UNAUTHORIZED" && e.Metadata != nil && e.Metadata["reason_code"] == "401"
}

// ErrorUnauthorized 创建 UNAUTHORIZED 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：无效身份
func ErrorUnauthorized(messageParts ...any) *errors.Error {
	message := "无效身份"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(401, "UNAUTHORIZED", message).WithReasonCode(401).WithErrStack(4)
}

// IsForbidden 判断 err 是否为 FORBIDDEN 错误。
// 业务说明：无权限操作
func IsForbidden(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "FORBIDDEN" && e.Metadata != nil && e.Metadata["reason_code"] == "403"
}

// ErrorForbidden 创建 FORBIDDEN 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：无权限操作
func ErrorForbidden(messageParts ...any) *errors.Error {
	message := "无权限操作"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(403, "FORBIDDEN", message).WithReasonCode(403).WithErrStack(4)
}

// IsNotFound 判断 err 是否为 NOT_FOUND 错误。
// 业务说明：资源不存在
func IsNotFound(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "NOT_FOUND" && e.Metadata != nil && e.Metadata["reason_code"] == "404"
}

// ErrorNotFound 创建 NOT_FOUND 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：资源不存在
func ErrorNotFound(messageParts ...any) *errors.Error {
	message := "资源不存在"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(404, "NOT_FOUND", message).WithReasonCode(404).WithErrStack(4)
}

// IsConflict 判断 err 是否为 CONFLICT 错误。
// 业务说明：资源状态冲突
func IsConflict(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "CONFLICT" && e.Metadata != nil && e.Metadata["reason_code"] == "409"
}

// ErrorConflict 创建 CONFLICT 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：资源状态冲突
func ErrorConflict(messageParts ...any) *errors.Error {
	message := "资源状态冲突"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(409, "CONFLICT", message).WithReasonCode(409).WithErrStack(4)
}

// IsValidator 判断 err 是否为 VALIDATOR 错误。
// 业务说明：请求字段校验不通过
func IsValidator(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "VALIDATOR" && e.Metadata != nil && e.Metadata["reason_code"] == "422"
}

// ErrorValidator 创建 VALIDATOR 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：请求字段校验不通过
func ErrorValidator(messageParts ...any) *errors.Error {
	message := "请求字段校验不通过"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(422, "VALIDATOR", message).WithReasonCode(422).WithErrStack(4)
}

// IsTooManyRequests 判断 err 是否为 TOO_MANY_REQUESTS 错误。
// 业务说明：请求次数过多
func IsTooManyRequests(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "TOO_MANY_REQUESTS" && e.Metadata != nil && e.Metadata["reason_code"] == "429"
}

// ErrorTooManyRequests 创建 TOO_MANY_REQUESTS 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：请求次数过多
func ErrorTooManyRequests(messageParts ...any) *errors.Error {
	message := "请求次数过多"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(429, "TOO_MANY_REQUESTS", message).WithReasonCode(429).WithErrStack(4)
}

// IsClientDisconnected 判断 err 是否为 CLIENT_DISCONNECTED 错误。
// 业务说明：client has closed connection
func IsClientDisconnected(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "CLIENT_DISCONNECTED" && e.Metadata != nil && e.Metadata["reason_code"] == "499"
}

// ErrorClientDisconnected 创建 CLIENT_DISCONNECTED 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：client has closed connection
func ErrorClientDisconnected(messageParts ...any) *errors.Error {
	message := "client has closed connection"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(499, "CLIENT_DISCONNECTED", message).WithReasonCode(499).WithErrStack(4)
}

// IsInternalServer 判断 err 是否为 INTERNAL_SERVER 错误。
// 业务说明：服务器错误
func IsInternalServer(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "INTERNAL_SERVER" && e.Metadata != nil && e.Metadata["reason_code"] == "500"
}

// ErrorInternalServer 创建 INTERNAL_SERVER 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：服务器错误
func ErrorInternalServer(messageParts ...any) *errors.Error {
	message := "服务器错误"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(500, "INTERNAL_SERVER", message).WithReasonCode(500).WithErrStack(4)
}

// IsNotImplemented 判断 err 是否为 NOT_IMPLEMENTED 错误。
// 业务说明：API 未实现
func IsNotImplemented(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "NOT_IMPLEMENTED" && e.Metadata != nil && e.Metadata["reason_code"] == "501"
}

// ErrorNotImplemented 创建 NOT_IMPLEMENTED 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：API 未实现
func ErrorNotImplemented(messageParts ...any) *errors.Error {
	message := "API 未实现"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(501, "NOT_IMPLEMENTED", message).WithReasonCode(501).WithErrStack(4)
}

// IsBadGateway 判断 err 是否为 BAD_GATEWAY 错误。
// 业务说明：网关转发到下游失败
func IsBadGateway(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "BAD_GATEWAY" && e.Metadata != nil && e.Metadata["reason_code"] == "502"
}

// ErrorBadGateway 创建 BAD_GATEWAY 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：网关转发到下游失败
func ErrorBadGateway(messageParts ...any) *errors.Error {
	message := "网关转发到下游失败"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(502, "BAD_GATEWAY", message).WithReasonCode(502).WithErrStack(4)
}

// IsServiceUnavailable 判断 err 是否为 SERVICE_UNAVAILABLE 错误。
// 业务说明：服务不可用
func IsServiceUnavailable(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "SERVICE_UNAVAILABLE" && e.Metadata != nil && e.Metadata["reason_code"] == "503"
}

// ErrorServiceUnavailable 创建 SERVICE_UNAVAILABLE 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：服务不可用
func ErrorServiceUnavailable(messageParts ...any) *errors.Error {
	message := "服务不可用"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(503, "SERVICE_UNAVAILABLE", message).WithReasonCode(503).WithErrStack(4)
}

// IsGatewayTimeout 判断 err 是否为 GATEWAY_TIMEOUT 错误。
// 业务说明：下游响应超时
func IsGatewayTimeout(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "GATEWAY_TIMEOUT" && e.Metadata != nil && e.Metadata["reason_code"] == "504"
}

// ErrorGatewayTimeout 创建 GATEWAY_TIMEOUT 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：下游响应超时
func ErrorGatewayTimeout(messageParts ...any) *errors.Error {
	message := "下游响应超时"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(504, "GATEWAY_TIMEOUT", message).WithReasonCode(504).WithErrStack(4)
}

// IsHTTPVersionNotSupported 判断 err 是否为 HTTP_VERSION_NOT_SUPPORTED 错误。
// 业务说明：服务器不支持请求中使用的 HTTP 版本
func IsHTTPVersionNotSupported(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "HTTP_VERSION_NOT_SUPPORTED" && e.Metadata != nil && e.Metadata["reason_code"] == "505"
}

// ErrorHTTPVersionNotSupported 创建 HTTP_VERSION_NOT_SUPPORTED 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：服务器不支持请求中使用的 HTTP 版本
func ErrorHTTPVersionNotSupported(messageParts ...any) *errors.Error {
	message := "服务器不支持请求中使用的 HTTP 版本"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(505, "HTTP_VERSION_NOT_SUPPORTED", message).WithReasonCode(505).WithErrStack(4)
}

// IsNetworkConnectTimeoutError 判断 err 是否为 NETWORK_CONNECT_TIMEOUT_ERROR 错误。
// 业务说明：网络连接超时
func IsNetworkConnectTimeoutError(err error) bool {
	if err == nil {
		return false
	}
	e := errors.FromError(err)
	if e == nil {
		return false
	}
	return e.Reason == "NETWORK_CONNECT_TIMEOUT_ERROR" && e.Metadata != nil && e.Metadata["reason_code"] == "599"
}

// ErrorNetworkConnectTimeoutError 创建 NETWORK_CONNECT_TIMEOUT_ERROR 错误；无参数使用 Proto 注释，单参数保留原文。
// 为兼容旧调用，字符串首参数带后续参数时按 fmt.Sprintf 格式化；新代码可先显式格式化消息。
// 业务说明：网络连接超时
func ErrorNetworkConnectTimeoutError(messageParts ...any) *errors.Error {
	message := "网络连接超时"
	if len(messageParts) > 0 {
		message = fmt.Sprint(messageParts...)
		if format, ok := messageParts[0].(string); ok && len(messageParts) > 1 {
			message = fmt.Sprintf(format, messageParts[1:]...)
		}
	}
	return errors.New(599, "NETWORK_CONNECT_TIMEOUT_ERROR", message).WithReasonCode(599).WithErrStack(4)
}
